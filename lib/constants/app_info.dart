class AppInfo {
  // Update these to match your app and contact.
  static const String appName = 'VRCMX';
  static const String appVersion = '1.0.0';
  // Recommended: email or URL where you can be reached.
  static const String contact = 'kcalb456@gmail.com';

  // VRChat guideline: include app name, version, and contact info.
  static const String userAgent = '$appName/$appVersion (contact: $contact)';
}
